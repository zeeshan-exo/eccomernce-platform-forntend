import React from "react";
// import { useGetCategoryQuery } from "../../features/auth/categorySlice";

function Categories() {
  //   const { data: categories, isLoading, error } = useGetCategoryQuery();

  //   if (isLoading) {
  //     return <div className="text-center mt-10">Loading Categories....</div>;
  //   }
  //   console.log(data);
  return <div>Right now no data to display</div>;
}

export default Categories;
